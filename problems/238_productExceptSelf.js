/**
 * @param {number[]} nums
 * @return {number[]}
 */
var productExceptSelf = function(nums) {
    var output = [];

    for(var i = 0; i < nums.length; i++) {
        for (var j = 0; j < nums.length; i++) {
            if (j != i) {
                
            }
        }
    }
};